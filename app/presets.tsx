import { PlaceholderScreen } from "../src/components/PlaceholderScreen";

export default function PresetsRoute() {
  return <PlaceholderScreen title="Presets" body="Seus presets salvos aparecerão aqui." />;
}
